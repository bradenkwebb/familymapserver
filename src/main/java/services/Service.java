package services;

/**
 * An interface for the services. Currently unnecessary (as it's empty), but I want to keep it in case I decide to
 * add functionality common to all classes.
 */
public interface Service {
}
