package requests;

/**
 * At the moment, this is a truly empty interface - but I think it's worth keeping in case I notice that there
 * really is something common to all requests that needs to be preserved
 */
public interface Request {
}
